[실행용 축약 템플릿 - 3단계 화면 설계 + 사용자 플로우]

이번에는 [3단계]만 수행해줘.
아직 다음 단계는 수행하지 말아줘.

[Canonical Decision Register]
(최신 frozen_values만 남기고 붙여넣기)

[이전 단계 authoritative_updates]
(직전 단계 YAML 그대로 붙여넣기)

[이전 단계 확정본 요약]
- 이번 단계 확정 사항:
- 다음 단계 입력으로 넘길 핵심 요약:

[이번 단계 입력 체크리스트]
- 2단계 상세 기능 명세서
- P0 화면 범위
- Canonical Decision Register

[이번 단계 추가 입력]
- 새로 확정할 값:
- 변경할 값:
- deprecated 처리할 값:
- 유지할 frozen_values:
- 추가 제약/예외:
- 운영/보안/권한 관련 메모:

[이번 단계 프롬프트]
3단계 요구사항만 수행해줘.

[출력에 반드시 포함할 섹션]
- 화면 ID
- 화면명
- 목적
- 진입 조건
- 진입 경로
- 주요 UI 구성
- 주요 인터랙션
- CTA 버튼
- 빈 상태
- 로딩 상태
- 오류 상태
- 성공 상태
- 다음 화면 이동 조건

- 이번 단계 확정 사항
- 다음 단계 입력으로 넘길 핵심 요약
- 추가 확인 필요 항목
- 충돌 가능 항목
- 바로 다음 단계 진행 가능 항목
- 단계 상태
- authoritative_updates YAML

[출력 고정 규칙]
- 섹션 제목과 순서를 그대로 유지할 것
- 비어 있는 섹션은 생략하지 말고 `없음`으로 적을 것
- 동일 성격의 목록은 별도 우선순위가 없으면 오름차순으로 정렬할 것
- authoritative_updates YAML은 반드시 ```yaml fenced block```으로 감쌀 것

[authoritative_updates YAML 템플릿]
authoritative_updates:
  frozen_values:
    key: value
  supersedes:
    - old_source -> new_source
  deprecated:
    - deprecated_key_or_rule
  carry_forward_only:
    - key1
    - key2
  do_not_carry_forward:
    - key3
    - key4

stage_status:
  maturity: draft|provisional|minimum_complete|fully_verified
  unresolved_conflicts:
    - none
  missing_p0_items:
    - none
